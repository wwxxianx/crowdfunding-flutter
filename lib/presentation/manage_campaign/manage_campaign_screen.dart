import 'package:flutter/material.dart';

class ManageCampaignScreen extends StatelessWidget {
  const ManageCampaignScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Manage")),
    );
  }
}
